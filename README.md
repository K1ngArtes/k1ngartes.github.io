# Run webiste locally using drafts
```
hugo server -D
```

# Development and deployment
Create new post using the default architype using
```
hugo new content posts/my-new-post.md
```

Generate the static data in `public` folder by running
```
hugo
```